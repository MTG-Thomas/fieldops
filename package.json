{
  "name": "fieldops",
  "version": "1.0.0",
  "private": true,
  "description": "HaloPSA Field Tech PWA",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed",
    "serve": "serve . -l 5000 --no-clipboard"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.0",
    "serve": "^14.2.3"
  }
}
